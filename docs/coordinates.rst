.. _coordinates:

===========
Coordinates
===========
ndcube supports the integration of data and coordinates in a number of ways.  In this section we will discuss convenice methods of the `~ndcube.NDCube` and `~ndcube.NDCubeSequence` classes.  But first we will highlight the power of two other classes provided by ndcube specifically designed for handling coordinates.

ExtraCoords
===========
